You'll need to create a ~/.vimrc (and possibly ~/.gvimrc) file for vim
to find.  I suggest a symlink:

  $ ln -s ~/.vim/vimrc.vim ~/.vimrc
  $ ln -s ~/.vim/gvimrc.vim ~/.gvimrc

Next, the Vundle bundles need to be installed (within vim):

  :BundleInstall

